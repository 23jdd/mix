# mix

```text

let name = "mix";

fn add(a, b) {
    return a + b;
}

if name == "Rust" {
    print(add(10, 20));
}

for i in range(0, 10) {
    print(i);
}
```



1. 基础类型：`int / float / bool / string / null/obj`
2. 变量：`let x = 10`
3. 运算：`+ - * / == != < >`
4. `if / else`
5. `while`
6. function
7. `return`
8. builtin：`print()`、`len()`
9. array
10. 错误信息
11. loop

